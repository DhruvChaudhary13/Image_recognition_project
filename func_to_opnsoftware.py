# import subprocess

# subprocess.run(["chrome.exe"])

import os

# Get the current working directory
cwd = os.getcwd()
print(cwd)

# Run a system command (e.g., open a file or run software)
os.system('notepad.exe')

